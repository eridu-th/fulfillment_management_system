export default {
    auth(state) {
        return state.auth;
    },
    token(state) {
        return state.token;
    },
    error(state) {
        return state.error;
    },
    endpoints(state) {
        return state.endpoints;
    },
    user(state) {
        return state.user;
    }
}